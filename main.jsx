import React from 'react';
import ReactDOM from 'react-dom/client';
import WeddingTracker from './WeddingTracker.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WeddingTracker />
  </React.StrictMode>
);
